import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'

/**
 * Provides the MCP server's prompts.
 */
export abstract class Prompts {
	public static addAll(_server: McpServer) {
		// not implemented yet
	}
}
